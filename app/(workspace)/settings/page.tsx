import type { Metadata } from "next"; import { ShieldCheck, Users } from "lucide-react"; import { prisma } from "@/lib/prisma"; import { requireOrganization } from "@/lib/auth"; import { hasPermission } from "@/lib/permissions"; import { SettingsForm } from "@/components/settings-form"; import { TeamManager } from "@/components/team-manager";
export const metadata: Metadata = { title: "Settings" }; export const dynamic = "force-dynamic";
export default async function SettingsPage() { const context = await requireOrganization(); const canManage = hasPermission(context.role, "settings"); const members = await prisma.profile.findMany({ where: { organizationId: context.organizationId }, select: { id: true, fullName: true, email: true, role: true }, orderBy: { createdAt: "asc" } }); return <div className="page-stack"><header className="page-header"><div><p className="page-kicker">WORKSPACE GOVERNANCE</p><h1>Settings</h1><p>Manage organization preferences, roles and security posture.</p></div></header><section className="settings-grid"><article className="panel settings-card"><header><div><p className="page-kicker">ORGANIZATION</p><h2>Workspace details</h2></div><ShieldCheck /></header><SettingsForm organization={context.organization} canManage={canManage} /></article><article className="panel settings-card"><header><div><p className="page-kicker">ACCESS CONTROL</p><h2>Team members</h2></div><Users /></header><TeamManager members={members} canManage={canManage} currentRole={context.role} /><p className="settings-note">Supabase Auth provisions identities. Owners and administrators govern roles for profiles assigned to this organization; only owners can change owner access.</p></article></section></div>; }
